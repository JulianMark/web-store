# server app port
server:
  port: 9092

# config mongodb server local
spring:
  data:
    mongodb:
      host: LocalHost
      port: 27017
      authentication-database: none
      database: Shopping
